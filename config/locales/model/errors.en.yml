en:
  model:
    errors:
      user:
        name:
          blank: 'Specify the name'
      sleep_period:
        user_id:
          blank: 'Specify the user_id'
        wake_up:
          less_than_fall_asleep: 'Must be after fall_asleep'
      following:
        follower_id:
          blank: 'Specify the follower_id'
        followee_id:
          blank: 'Specify the followee_id'
          can_not_be_same_as_follower: 'Can not be same as the follower'

